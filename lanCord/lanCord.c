#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main()
{
	int k, n;
	scanf("%d %d", &k, &n);
	int* cord = (int*)malloc(k * sizeof(int));
	int a = 1;
	int maxCord = 0;

	for (int i = 0; i < k; i++)
		scanf("%d", cord[i]);

	while (1)
	{
		
	}

}